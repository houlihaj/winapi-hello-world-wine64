## Build

Using Ubuntu 24.04, mingw-w64, and wine64.

### Dependencies

```bash 
sudo apt install mingw-w64 mingw-w64-x86-64-dev wine64
```

### Compilation

``` bash
make
```

Run ``` make clean ```, then ``` make``` to rebuild from scratch.

### Run

```
wine main.exe
```

